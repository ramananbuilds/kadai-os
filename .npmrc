# Expo's Metro resolver works most reliably against a hoisted node_modules
# layout; pnpm's default symlinked store breaks transitive resolution for
# react-native packages. This is Expo's documented monorepo recommendation.
node-linker=hoisted
